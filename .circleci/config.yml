version: 2.1
jobs:
  build:
    docker:
      - image: fedora:latest
    steps:
      - checkout
      - run: 
          name: Install Build Tools
          command: dnf install ansible nc -y
      - run:
          name: Run Build
          command: export INSIDE_CI=True && ./userland/setup.sh