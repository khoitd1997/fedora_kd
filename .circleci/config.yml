version: 2.1
jobs:
  build:
    docker:
      - image: fedora:latest
    steps:
      - checkout
      - run: 
          name: Install Build Tools
          command: dnf install ansible -y
      - run:
          name: Run Build
          command: ./userland/setup.sh