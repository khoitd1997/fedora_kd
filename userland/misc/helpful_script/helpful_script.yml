- block:
    - name: linking helpful scripts
      shell: "{{item}}"
      with_items:
        - ln -sfv {{ playbook_dir }}/misc/helpful_script/scripts/* ~/.local/bin
      args:
        warn: false
  tags:
    - helpful_script
