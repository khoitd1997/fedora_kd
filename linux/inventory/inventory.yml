all:
  hosts:
    kd-server:
    localhost:
      ansible_connection: local
  vars:
    ansible_python_interpreter: "/usr/bin/python3"
    bulk_storage_dir: "/bulk-storage"
    backup_dir: "{{ bulk_storage_dir }}/backup"
    bulk_storage_share_dir: "{{ bulk_storage_dir }}/nfs/general"